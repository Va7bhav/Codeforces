#include<iostream>
#include<bits/stdc++.h>
using namespace std;

int main() {
    int t; cin >> t;
    while (t--) {
        int n, p, l, t; cin >> n >> p >> l >> t;
        int days = ceil(p / l);

    }
    return 0;
}